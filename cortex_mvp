#!/bin/bash
# Cortex MVP CLI Wrapper
# Usage: ./cortex_mvp <command> [args]

CORTEX_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$CORTEX_DIR"

# Activate venv if it exists
if [ -d "venv" ]; then
    source venv/bin/activate
fi

# Run CLI
python mvp/cli.py "$@"
