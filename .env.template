# Cortex Environment Variables Template
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to git!

# Anthropic API (REQUIRED for batch analysis and bridge intelligence)
# ANTHROPIC_API_KEY=sk-ant-...

# Cortex Configuration (Optional - defaults shown)
CORTEX_ROOT_DIR=/path/to/your/projects
# CORTEX_ANTI_PATTERNS_SCRIPT=/path/to/mine_anti_patterns.py  # Optional: enables mine-anti-patterns CLI

# Alternative: Store API key in file (recommended)
# mkdir -p ~/.cortex/secrets
# echo "YOUR_KEY" > ~/.cortex/secrets/anthropic_batch_key
# chmod 600 ~/.cortex/secrets/anthropic_batch_key
